<UserControl x:Class="DeviceTesterUI.Views.DeviceListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:DeviceTesterUI.Converters"
             Background="White">
    <UserControl.Resources>
        <conv:BoolToBrushConverter x:Key="BoolToBrushConverter"/>
    </UserControl.Resources>
    <Grid>
        <DataGrid x:Name="DeviceDataGrid"
          ItemsSource="{Binding Devices}"
          SelectedItem="{Binding SelectedDevice, Mode=OneWayToSource}"
          AutoGenerateColumns="False"
          HeadersVisibility="Column"
          CanUserAddRows="False"
          Margin="5"
          FontSize="10"
          ScrollViewer.VerticalScrollBarVisibility="Auto"
          ScrollViewer.HorizontalScrollBarVisibility="Auto"
          IsReadOnly="True"
          GridLinesVisibility="None"
                  BorderBrush="LightGray">

            <!-- Header padding -->
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Padding" Value="5"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <!-- Row cell padding (for TextColumns) -->
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <!--<Setter Property="BorderThickness" Value="0"/>-->
                    <Setter Property="BorderBrush" Value="LightGray"/>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Agent" Binding="{Binding Agent}" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Buttons column -->
                <DataGridTemplateColumn Header="Common Operations" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <UniformGrid Rows="1" Columns="2" HorizontalAlignment="Left" Margin="5,2">
                                <Button Content="Authenticate" Click="Authenticate_Click" Padding="5,2" Margin="2,0" />
                                <Button Content="Delete" Click="Delete_Click" Padding="5,2" Margin="2,0"/>
                            </UniformGrid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Authenticated" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Ellipse Width="12" Height="12" VerticalAlignment="Center"
                     Fill="{Binding IsAuthenticated, Converter={StaticResource BoolToBrushConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
